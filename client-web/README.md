# DevTinder - Web App

The project is created using Vite + React

## Configurations

### Installations

1. Install Vite

   ```sh
   npm create vite@latest client-web -- --template react-ts
   ```

2. Run the app

   ```
    cd client-web
    npm install
    npm run dev
   ```
