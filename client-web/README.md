# DevTinder - Web App

The project is created using Vite + React

## Configurations

### Installations

1. Install Vite

   ```sh
   npm create vite@latest devtinder-web -- --template react-ts
   ```

2. cd devtinder-web
   npm install
   npm run dev
